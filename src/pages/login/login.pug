extends ../layout/layout

include ../../components/card/sign-in/sign-in

block vars
  - const title = 'Sign in'
  - const bodyClasses = ['clearfix']

block main
  article.login
    section.login__form
      +sign-in