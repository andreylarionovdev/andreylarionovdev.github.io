include ../checkbox/checkbox

mixin checkbox-list-expandable(options)
  - const { id, label, isExpanded, items = [] } = options || {};

  - const blockModifiers = isExpanded ? ['expanded'] : [];

  - const createBemClasses = require('../../utils/helpers').createBemClasses;
  - const blockName = 'checkbox-list-expandable';
  - const jsPrefix = 'js';

  div(id=id class=createBemClasses({ blockName, modifiers: blockModifiers, jsPrefix }))
    p.checkbox-list-expandable__group
      span.checkbox-list-expandable__label= label
      button(class=createBemClasses({ blockName, elementName: 'toggle-button', jsPrefix })).material-icons(type='button')= 'keyboard_arrow_down'
    ul.checkbox-list-expandable__list
      each item, index in items
        li.checkbox-list-expandable__item
          +checkbox(item)