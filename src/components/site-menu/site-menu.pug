mixin site-menu(options)
  - var { theme, items } = options || {}

  ul.site-menu(class=`site-menu--${theme}`)
    each link in items
      li.site-menu__item
        h2.site-menu__title
          a.site-menu__link(href=link.href)= link.text