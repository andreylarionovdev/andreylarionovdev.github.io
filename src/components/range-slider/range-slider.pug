include ../label/label

mixin range-slider(options)
  - const b = 'range-slider'
  - const caption = options.caption || 'range slider'
  - const { type, skin, min, max, from, to } = options

  div(class=b)
    +label({
      label: caption
    })
    input(
      class=`${b}__input`
      type='text'
      name=options.name
      value=''
      data-type=type
      data-skin=skin
      data-min=min
      data-max=max
      data-from=from
      data-to=to
    )
