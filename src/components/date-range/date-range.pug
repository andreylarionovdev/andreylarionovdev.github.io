include ../label/label
include ../dropdown/dropdown

mixin date-range(options)
  -
    var mask = 'ДД.ММ.ГГГГ'
    var {
      id,
      fromLabel = 'Прибытие',
      from = mask,
      toLabel = 'Выезд',
      to = mask
    } = options || {}

    var f = require('../../utils/functions')
    var b = 'date-range'
    var js = 'js'

  div(class=f.bem({b, js}))
    div(class=f.bem({b, e: 'from', js}))
      .date-range__label
        +label({ label: fromLabel })
      div(class=f.bem({b, e: 'dropdown', js}))
        +dropdown({id: `${id}__from-date`, value: from})
    div(class=f.bem({b, e: 'to', js}))
      .date-range__label
        +label({ label: toLabel })
      div(class=f.bem({b, e: 'dropdown', js}))
        +dropdown({id: `${id}__to-date`, value: to})