mixin rate-button(options)
  - const b = 'rate-button'
  - const { id, base, rate, name = id } = options || {}

  div(class='rate-button' id=id)
    - let n = base
    while n > 0
      input(
        id=id + n
        type='radio'
        class=`${b}__radio`
        name=name
        value=n
        checked=(n === rate)
      )
      label(
        for=id + n
        class=`${b}__label`
      )= n
      - n = n - 1