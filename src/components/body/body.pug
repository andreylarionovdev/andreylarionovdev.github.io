include ../../components/logo/logo

mixin body(options)
  - const { theme } = options || {};
  - const f = require('../../utils/functions');

  body(class=f.bem({ b: 'body', m: [{ theme }] }))
    block