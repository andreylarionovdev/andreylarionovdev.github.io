include ../rating/rating

mixin room-thumbnail(options)
  -
    const {
      roomNumber,
      href='change-me',
      pricePerDay,
      feedbackCounter,
      rate,
      images,
      feature,
      focus,
    } = options || {};

    const blockModifiers = focus ? ['focused'] : [];

    const helpers = require('../../utils/helpers');

    const createBemClasses = helpers.createBemClasses;
    const formatCurrency = helpers.formatCurrency;

    const blockName = 'room-thumbnail';

  div(class=createBemClasses({ blockName, modifiers: blockModifiers}))
    figure.room-thumbnail__carousel
      each image, inx in images
        - const bemObj = { blockName, elementName: 'carousel-image'}
        - bemObj.modifiers = inx === 0 ? ['active'] : [];
        img(class=createBemClasses(bemObj) src=image.src alt=image.alt)
      button(type='button' class=createBemClasses({ blockName, elementName: 'carousel-control', modifiers: [{ type: 'prev' }] }))
        span.material-icons= 'keyboard_arrow_left'
      button(type='button' class=createBemClasses({ blockName, elementName: 'carousel-control', modifiers: [{ type: 'next' }] }))
        span.material-icons= 'keyboard_arrow_left'
      div.room-thumbnail__carousel-indicators
        each _, inx in images
          - const bemObj = { blockName, elementName: 'carousel-indicator'};
          - bemObj.modifiers = inx === 0 ? ['active'] : [];
          button(type='button' class=createBemClasses(bemObj))
    header.room-thumbnail__header
      a(href=href).room-thumbnail__number
        span.room-thumbnail__number-sign= '№'
        span.room-thumbnail__number-value= roomNumber
        if feature
          span.room-thumbnail__number-feature= feature
      .room-thumbnail__price-per-day
        span.room-thumbnail__cost!= formatCurrency({value: pricePerDay})
        |&nbsp;
        span.room-thumbnail__date-unit= 'в сутки'
    footer.room-thumbnail__footer
      .room-thumbnail__rate
        +rating({
          id: `${roomNumber}__rate`,
          base: 5,
          rate,
        })
      p.room-thumbnail__feedbacks-counter
        span.room-thumbnail__feedbacks-counter-number= feedbackCounter
        |&nbsp;
        span.room-thumbnail__feedbacks-counter-text= 'Отзывов'