mixin button(theme, caption, options)
  - var blockName = 'button'
  - var modifiers = [blockName + '--' + theme]
  - var href = 'javascript:void(0);'
  - var withArrow = false

  if options
    if options.hover
      - modifiers.push(blockName + '--hovered')
    if options.href
      - href = options.href
    if options.withArrow
      - withArrow = options.withArrow

  a(
    class=blockName
    class=modifiers
    href=href
  )
    if withArrow
      span.button__arrow
        = caption
        i.material-icons.arrow= 'arrow_forward'
    else
      = caption