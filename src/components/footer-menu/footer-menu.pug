mixin footer-menu(options)
  - var { title, menuItems } = options || {}

  .footer-menu
    h3.footer-menu__title.h3= title
    ul.footer-menu__list
      each item, _ in menuItems
        li.footer-menu__item
          a.footer-menu__link(href=item.href).body= item.text